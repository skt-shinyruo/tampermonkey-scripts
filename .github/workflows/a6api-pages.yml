name: Publish A6API Userscript

on:
  push:
    branches:
      - main
    paths:
      - 'a6api/**'
      - '.github/workflows/a6api-pages.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: userscripts-publish
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Check source syntax
        run: node --check a6api/a6api-fixed-exchange-rate.user.js

      - name: Check build script syntax
        run: node --check a6api/build-userscript.mjs

      - name: Run tests
        run: node --test a6api/a6api-fixed-exchange-rate.user.test.mjs

      - name: Build userscript
        run: |
          script_url="https://raw.githubusercontent.com/${GITHUB_REPOSITORY}/build/a6api-fixed-exchange-rate.user.js"
          A6API_VERSION="0.2.${GITHUB_RUN_NUMBER}" \
          A6API_UPDATE_URL="$script_url" \
          A6API_DOWNLOAD_URL="$script_url" \
          node a6api/build-userscript.mjs --output=dist/a6api-fixed-exchange-rate.user.js

      - name: Check userscript syntax
        run: node --check dist/a6api-fixed-exchange-rate.user.js

      - name: Publish userscript to build branch
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          publish_dir="$(mktemp -d)"
          cp dist/a6api-fixed-exchange-rate.user.js "$publish_dir/a6api-fixed-exchange-rate.user.js"

          git fetch origin build:refs/remotes/origin/build || true
          if git show-ref --verify --quiet refs/remotes/origin/build; then
            git switch --detach origin/build
            if [ -f linuxdo-open-links-new-tab.user.js ]; then
              cp linuxdo-open-links-new-tab.user.js "$publish_dir/linuxdo-open-links-new-tab.user.js"
            fi
            if [ -f sub2api-helper.user.js ]; then
              cp sub2api-helper.user.js "$publish_dir/sub2api-helper.user.js"
            fi
          else
            git switch --orphan build
            git rm -rf . || true
          fi

          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          if [ -f "$publish_dir/linuxdo-open-links-new-tab.user.js" ]; then
            cp "$publish_dir/linuxdo-open-links-new-tab.user.js" .
          fi
          if [ -f "$publish_dir/sub2api-helper.user.js" ]; then
            cp "$publish_dir/sub2api-helper.user.js" .
          fi
          cp "$publish_dir/a6api-fixed-exchange-rate.user.js" .

          git add -A
          if git diff --cached --quiet; then
            echo "No build branch changes to publish."
          else
            git commit -m "chore: publish a6api userscript build"
            git push origin HEAD:build
          fi
